# Development environment variables
# Copy this to .env and customize for your environment

# Redis
REDIS_URL=redis://localhost:6379

# PostgreSQL
DATABASE_URL=postgresql://flexype:hackathon2026@localhost:5432/inventory_system

# JWT
JWT_SECRET=your-super-secret-key-change-in-production-please-use-strong-random-key
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=15

# Business Logic
RESERVATION_TTL_SECONDS=300
MAX_QUANTITY_PER_RESERVE=5
MIN_QUANTITY_PER_RESERVE=1

# Rate Limiting
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_IP_MINUTE=100

# Worker
EXPIRY_CHECK_INTERVAL_SECONDS=10

# Debug
DEBUG=True
